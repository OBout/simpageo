var geo = geo || {};
var constants = {
	debug: true,
	googleurl: "http://oscoweb.com"
};

function db(l2p){
	var debug = constants.debug;
	if (debug === true) {
			console.log(l2p);
		} else {
			console.log("helash");
		}
};